# NestSuite 利用ガイド

NestSuite は、NoteNest / ChatNest / IdeaNest の 3 ツールを 1 つのシェル上で並行利用できる試験統合版インターフェースです（v1.10.0）。

---

## 起動方法

### NoteNest 単体版（従来どおり）

```
NoteNest.exe
NoteNest.exe sample.notenest
```

- 引数なし、または `.notenest` ファイル指定では NoteNest 単体版（`MainWindow`）として起動します
- 従来の動作をそのまま維持しています

### NestSuite として起動

```
NoteNest.exe --nestsuite
NoteNest.exe --nestsuite sample.notenest
NoteNest.exe --nestsuite sample.chatnest
NoteNest.exe --nestsuite sample.ideanest
```

- `--nestsuite` を付けることで NestSuite として起動します
- ファイルを指定した場合、そのファイルだけをタブとして開きます（余分な無題タブは作成されません）
- ファイルを指定しない場合、無題の NoteNest タブから始まります
- 未対応拡張子や読込失敗時はエラーを表示し、必要に応じて無題 NoteNest タブへフォールバックします

---

## 3 ツールとファイル形式

| ツール | ファイル拡張子 | 内容 |
|--------|--------------|------|
| NoteNest | `.notenest` | ノート・タスク・マーカーを JSON で管理するプロジェクトファイル（スキーマ 1.4.1） |
| ChatNest | `.chatnest` | チャット形式のメモ（JSON） |
| IdeaNest | `.ideanest` | アイデアカード集（JSON） |

各ツールの保存形式は互いに独立しています。共通プロジェクトファイル形式は現時点では未導入です。

---

## タブの考え方

NestSuite のタブは**ツール単位ではなく、ファイル／作業単位**で作成されます。

```
例：[業務改善.notenest] [会議メモ.chatnest] [アイデア整理.ideanest] [B.notenest]
```

- 同じツールのタブを複数同時に開けます（例：NoteNest を 2 タブ）
- タブごとに独立した ViewModel を持ち、内容が他のタブに混ざることはありません

---

## 基本操作

### 新規作成

サイドバーまたはファイルメニュー → 新規 から、各ツールの新規タブを作成できます。

| 操作 | 結果 |
|------|------|
| ファイル → 新規（NoteNest タブ選択中） | 新規 NoteNest タブを作成 |
| ファイル → 新規（ChatNest タブ選択中） | 新規 ChatNest タブを作成 |
| ファイル → 新規（IdeaNest タブ選択中） | 新規 IdeaNest タブを作成 |

### ファイルを開く

| 操作 | 結果 |
|------|------|
| ファイル → 開く（NoteNest タブ選択中） | `.notenest` ファイルを新しいタブで開く |
| ファイル → 開く（ChatNest タブ選択中） | `.chatnest` ファイルを新しいタブで開く |
| ファイル → 開く（IdeaNest タブ選択中） | `.ideanest` ファイルを新しいタブで開く |

- 同じファイルが既に開かれている場合は、新しいタブを作成せず既存タブをアクティブにします

### 保存

| 操作 | 結果 |
|------|------|
| ファイル → 上書き保存 | 選択中タブのみ保存 |
| ファイル → 名前を付けて保存 | 選択中タブを別名で保存 |

- 他のタブの状態には影響しません
- 名前を付けて保存で、別タブで既に開かれているファイルパスを指定した場合はエラーを表示し保存を中止します

### タブを閉じる

- タブの「×」ボタンをクリックします
- 未保存の変更がある場合、確認ダイアログを表示します（タブ名が明示されます）
- キャンセルするとタブは残ります
- 最後のタブを閉じると、無題の NoteNest タブが自動作成されます

---

## タブのツールチップ

タブにマウスを合わせると、以下の情報が表示されます：

```
種類: NoteNest
ファイル: C:\work\業務改善.notenest
状態: 保存済み
```

未保存タブの場合：
```
種類: ChatNest
ファイル: 未保存（無題）
状態: 保存済み
```

---

## 既知の制約

詳細は [docs/nestsuite-known-limitations.md](nestsuite-known-limitations.md) を参照してください。

主な制約：
- NestSuite はまだ試験統合版です（`--nestsuite` による明示的な起動が必要）
- タブ復元は未対応（アプリ再起動後のタブは復元されません）
- 最近ファイル統合は未対応（各ツールの最近ファイルはまだ個別管理です）
- 複数ファイルの一括オープンは未対応

---

## 実機確認チェックリスト

詳細は [docs/test-scenarios.md](test-scenarios.md) の「v1.10.0 NestSuite 統合版チェックリスト」を参照してください。
