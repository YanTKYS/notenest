# NestSuite リリース前確認チェックリスト（v2.7.14）

NestSuite のリリース前に確認する項目を整理します。v1.19.3 で `--classic-notenest` を削除し、v1.21.0 からアプリ表示名・EXE 名を NestSuite に統一しています。v2.0.0 で NestSuite 正式リリース、v2.0.1 でリポジトリ名変更前の docs 整理、v2.1.0 で IdeaNest インライン編集、v2.1.1 で IdeaNest 編集導線を統一、v2.1.2 で細部修正、v2.1.3 でフッター表示の文言整理、v2.2.0 でタブ新規追加ボタン（SH-5）・Workspace 種別カラーアクセント（SH-7）・未保存マーク改善、v2.3.0 で ChatNest 自動スクロール（CH-3）・削除確認ダイアログ（CH-4）・インライン編集（CH-6）、v2.3.1 で ChatNest / IdeaNest ViewModel Dispose 実装（TD-1）、v2.3.2 で無効状態ボタンのカーソル・視覚フィードバック統一（SH-11）、v2.4.0 でタブコンテキストメニュー（SH-2）・中クリックで閉じる（SH-3）・タブ切替キーボードショートカット（SH-4）、v2.4.1 で NoteNest ノートタイトル絞り込み（L1/L2）、v2.4.2 で IdeaNest 詳細ウィンドウのサイズ・位置記憶（ID-3）、v2.4.3 で NoteNest 完了済みタスクの薄表示・折り畳み（L7）、v2.4.4 でメニュー・ダイアログのアクセスキー完全化（SH-12）、v2.4.5 で NoteNest 右ペインのタスク・マーカー件数バッジ（L6）、v2.4.6 でステータスバーの Workspace 別情報表示（SH-8）、v2.5.0 で検索／置換の件数表示・前後移動・全ノート検索（M1）、v2.5.1 で NoteNest エディタ TextBox 依存棚卸し（H0-1）、v2.5.2 で ITextEditorAdapter 設計（H0-2）、v2.5.3 で TextBoxEditorAdapter 試験実装（H0-3）、v2.5.4 で EditorHost 導入方針整理（H0-4）、v2.5.5 で H1〜H4 実装方式の再判定（H0-5）、v2.5.6 で NestSuite 開発ルール文書化（TD-8）、v2.5.7 で NoteEditorHost 最小実装（EH-1）、v2.5.8 で行番号ガターの現在行強調（H2a）、v2.5.9 で簡易ノートリンク補完（H1a）、v2.5.10 で NoteNest Workspace 行番号既定表示回帰修正、v2.6.0 で TempNest 固定タブ最小実装、v2.6.1 で TempNest 初期改善（SH-16 / TN-1 / TD-9）、v2.6.2 で品質改善・空状態ガイド・テスト拡充（TN-5 / TN-6 / L13 / ID-11 / TD-10 / TD-12）、v2.6.3 で開発規約にプロンプト標準契約を追記、v2.6.4 でタブ表示整理・ツールチップ改善（SH-14）、v2.6.5 でタブドラッグ挿入位置インジケーター（SH-17）、v2.6.6 で NoteNest ノート複製（L11）、v2.6.7 で NoteNest リンク切れ手動チェック（M11）、v2.6.8 で NoteNest ノート名変更時リンク影響警告（M9）、v2.6.9 で NoteNest リンク管理タブ（M3）、v2.7.0 で .NET Framework 4.8 テストビルド追加（GitHub Actions）、v2.7.1 で net48_test 互換性エラーの最小修正、v2.7.2 で net48_test 残存非互換 API の一括修正、v2.7.3 で net48_test 検証終了・正式採用保留・通常リリース成果物から除外、v2.7.4 で NestSuiteShellWindow.xaml.cs を責務別 partial ファイルへ分割（TD-2）、v2.7.5 で例外診断改善・Error ログ最小導入（TD-4 / TD-5 縮小採用）、v2.7.13 で終了・タブクローズ確認フローのテスト容易化（TD-7）、v2.7.14 で Tab と Session の変換境界整理（TD-6 第一段階）を実施しました。

> **実装時は `docs/development/nestsuite-development-guidelines.md` を参照してください。**

---

## 1. ビルド・テスト

- [ ] `dotnet build` が通る
- [ ] `dotnet test` が通る（全テスト）
- [ ] `ApplicationVersionTests` でバージョンが正しく表示されることを確認（v2.4.1）
- [ ] `NoteNest保存スキーマが 1.4.1` のままであることを確認

---

## 2. 起動確認（NestSuite 主対象）

- [ ] `NestSuite.exe` → NestSuite として起動し、Temp タブがアクティブになる（v2.6.0 / 無題 NoteNest タブは作成されない）
- [ ] `NestSuite.exe sample.notenest` → NestSuite で `.notenest` タブを開く
- [ ] `NestSuite.exe sample.chatnest` → NestSuite で `.chatnest` タブを開く
- [ ] `NestSuite.exe sample.ideanest` → NestSuite で `.ideanest` タブを開く
- [ ] `NestSuite.exe --nestsuite` → NestSuite として起動する（互換フラグ、フラグなしと同じ動作）
- [ ] `NestSuite.exe --nestsuite sample.notenest` → NestSuite で `.notenest` タブを開く

### `--classic-notenest` 廃止確認（v1.19.3）

> --classic-notenest は v1.19.3 で削除済み。classic NoteNest が起動しないことを確認する。

- [ ] `NestSuite.exe --classic-notenest` → classic NoteNest が起動**しない**（NestSuite が通常起動する）
- [ ] `NestSuite.exe --classic-notenest sample.notenest` → クラッシュせず、NestSuite で対象ファイルが開く

---

## 3. NestSuite 基本操作

### タブ作成

- [ ] NoteNest タブを新規作成できる
- [ ] ChatNest タブを新規作成できる
- [ ] IdeaNest タブを新規作成できる
- [ ] NoteNest タブを複数同時に開ける
- [ ] ChatNest タブを複数同時に開ける
- [ ] IdeaNest タブを複数同時に開ける

### ファイルを開く

- [ ] `.notenest` ファイルを開ける
- [ ] `.chatnest` ファイルを開ける
- [ ] `.ideanest` ファイルを開ける
- [ ] 同じ `.notenest` を再度開くと既存タブがアクティブになる
- [ ] 同じ `.chatnest` を再度開くと既存タブがアクティブになる
- [ ] 同じ `.ideanest` を再度開くと既存タブがアクティブになる

### 複数ファイル一括オープン（v1.16.0）

- [ ] 「開く...」ダイアログで複数ファイルを同時選択できる
- [ ] `.notenest` / `.chatnest` / `.ideanest` を混在して複数選択し、それぞれタブとして開ける
- [ ] 既に開いているファイルを含めても重複タブにならない（既存タブがアクティブになる）
- [ ] 一部のファイルが存在しない場合、他のファイルは開き、「一部のファイルを開けませんでした」が表示される
- [ ] 開いたファイルが最近使ったファイルに登録される

### 最近使ったファイル（v1.14.0 / v1.14.1）

- [ ] ファイルメニューに「最近使ったファイル」サブメニューが表示される
- [ ] ファイルを開くと最近使ったファイルの先頭に追加される（3 ツール横断）
- [ ] 同じファイルを再度開くと重複せず先頭に移動する
- [ ] 最近使ったファイルから既存タブのファイルを選ぶと既存タブがアクティブになり、履歴でも先頭へ移動する（v1.14.1）
- [ ] 「開く...」から既存タブのファイルを選んだ場合も、履歴の先頭へ移動する（v1.14.1）
- [ ] 最近使ったファイルから削除・移動済みのファイルを選ぶとエラーダイアログが表示され履歴から削除される
- [ ] 最近使ったファイルに未対応拡張子のパスが混入している場合、エラーダイアログが表示され履歴から削除される（v1.14.1）
- [ ] NoteNest タブで「名前を付けて保存」すると保存先が最近使ったファイルに追加される
- [ ] アプリ再起動後も最近使ったファイルが引き継がれる

### 保存

- [ ] NoteNest タブを上書き保存できる（メニュー・Ctrl+S 両方）
- [ ] ChatNest タブを上書き保存できる（メニュー・Ctrl+S 両方）
- [ ] IdeaNest タブを上書き保存できる（メニュー・Ctrl+S 両方）
- [ ] 保存後に未保存マーク（`*`）が消える
- [ ] 名前を付けて保存で別タブが開くパスを指定するとエラーが表示される

### Ctrl+S ショートカット（v1.16.1）

- [ ] NoteNest タブで Ctrl+S を押すと上書き保存される
- [ ] ChatNest タブで Ctrl+S を押すと上書き保存される
- [ ] IdeaNest タブで Ctrl+S を押すと上書き保存される
- [ ] 未保存（パス未設定）タブで Ctrl+S を押すと「名前を付けて保存」に進む
- [ ] メニューの「上書き保存」に Ctrl+S が自動表示される
- [ ] メニューの「上書き保存」クリックと Ctrl+S の挙動が一致する

### タブを閉じる

- [ ] 保存済みタブを閉じられる
- [ ] 未保存タブを閉じようとすると確認ダイアログが表示される（タブ名が明示されている）
- [ ] 確認でキャンセルするとタブが残る
- [ ] 確認で破棄するとタブが閉じる
- [ ] 最後の通常タブを閉じると Temp タブがアクティブになる（v2.6.0 / 無題 NoteNest タブは自動作成されない）

### タブ復元（v1.15.0）

- [ ] 保存済みファイルタブを開いた状態でアプリを終了し、再起動すると前回のタブが復元される
- [ ] `.notenest` / `.chatnest` / `.ideanest` の 3 形式すべてが復元される
- [ ] 前回アクティブだったタブが復元後に選択されている
- [ ] 未保存タブ（無題）は復元されない
- [ ] `NestSuite.exe sample.notenest` など引数指定起動でも前回タブが復元され、指定ファイルが追加タブとして開く（v1.18.2）
- [ ] 引数指定ファイルが復元済みタブと同じ場合は重複タブを作らず既存タブがアクティブになる（v1.18.2）
- [ ] セッションに記録されたファイルが存在しない場合はスキップして他のファイルを復元する
- [ ] 復元対象が 1 件もない場合は Temp タブがアクティブになる（v2.6.0）

---

## 4. 独立性確認

- [ ] 複数の NoteNest タブで内容が混ざらない
- [ ] NoteNest と ChatNest と IdeaNest のタブで内容が混ざらない
- [ ] タブ A を保存してもタブ B の未保存状態に影響しない
- [ ] タブ A を編集してもタブ B の内容に影響しない

---

## 4.5. ツールランチャー表示確認（v1.16.2）

- [ ] ヘッダーバー左に「NestSuite」ラベルが表示される
- [ ] ヘッダーバー右側に NoteNest / IdeaNest / ChatNest のボタンが横並びで表示される
- [ ] 左側のツール選択パネルが表示されない（Workspace が全幅）
- [ ] NoteNest ボタンをクリックすると NoteNest タブへ移動または新規作成される
- [ ] IdeaNest ボタンをクリックすると IdeaNest タブへ移動または新規作成される
- [ ] ChatNest ボタンをクリックすると ChatNest タブへ移動または新規作成される
- [ ] アクティブなタブのツールに対応するボタンがハイライト表示される

---

## 4.6. IdeaNest 編集導線確認（v2.1.0 / v2.1.1）

### 詳細画面の基本操作

- [ ] カードをクリックすると詳細画面が開き、タイトル・本文・タグが即座に編集可能な状態で表示される
- [ ] 「✎ 編集」ボタンが表示されない
- [ ] タイトル TextBox に直接入力できる
- [ ] 本文 TextBox に直接入力でき、Enter で改行できる
- [ ] タグ TextBox にカンマ区切りでタグを入力できる
- [ ] カラーピッカー（色丸ボタン）を選択すると背景色が即座に変わる
- [ ] 「📌 ピン留め」ボタンをクリックすると ★ アイコンが表示され、ボタンラベルが「ピン留め解除」に変わる
- [ ] 「📥 アーカイブ」ボタンをクリックするとアーカイブ済みバッジが表示され、ボタンラベルが「アーカイブ解除」に変わる

### 保存タイミング

- [ ] 詳細画面を閉じると編集内容がカードに反映される（カード一覧のタイトル・色等が更新される）
- [ ] 「前へ」ボタンで前のカードへ移動すると、移動前のカードの編集内容が保存される
- [ ] 「次へ」ボタンで次のカードへ移動すると、移動前のカードの編集内容が保存される
- [ ] Ctrl+S を押すと即座に保存される（ウィンドウは閉じない）
- [ ] 何も変更せず閉じた場合、UpdatedAt が更新されない

### キーボード操作

- [ ] Escape キーで詳細画面を閉じる（編集内容は保存される）
- [ ] 本文 TextBox にフォーカスがある状態で ← / → キーを押してもカード移動しない（テキストカーソルが動く）
- [ ] 本文 TextBox にフォーカスがない状態で ← / → キーを押すとカードを移動する

### 旧編集ダイアログへの導線廃止確認（v2.1.1）

- [ ] カードにカーソルを合わせても「✎ 編集」ボタンが表示されない
- [ ] カードの右クリックコンテキストメニューに「編集」項目が表示されない
- [ ] 「＋」ボタンを押すと、旧編集ダイアログではなく詳細画面ベースの新規作成画面が開く
- [ ] 新規作成画面のタイトルが「新規アイデア」になっている
- [ ] 新規作成画面で前へ/次へボタンが無効になっている

### 新規作成確認（v2.1.1）

- [ ] 新規作成画面で日付行（作成日・更新日）が表示されない（v2.1.2）
- [ ] 新規作成画面でタイトル・本文・タグ・色・ピン留め・アーカイブを入力できる
- [ ] 何も入力せず閉じた場合、カードが追加されない
- [ ] 色を選んだだけ・ピン留めを押しただけで閉じた場合、カードが追加されない
- [ ] タイトルだけ入力して閉じると、カードが追加される
- [ ] 本文だけ入力して閉じると、カードが追加される
- [ ] タグだけ入力して閉じると、カードが追加される
- [ ] Ctrl+S を押すとカードが追加される
- [ ] Ctrl+S 後に閉じても同じカードが二重作成されない
- [ ] Ctrl+S 後に追加編集してから閉じると、追加編集分も保存される
- [ ] 再起動後も新規作成したカードが残っている

---

## 5. タブ表示確認

- [ ] タブ見出しに絵文字プレフィックスと拡張子なしファイル名が表示される（例：`📝 業務改善` / `💬 開発メモ` / `💡 ツール改修`）
- [ ] Temp タブの見出しは「Temp」のみ（絵文字プレフィックスなし）で表示される
- [ ] 未保存タブに `●` が表示される
- [ ] 保存後に `●` が消える
- [ ] タブのツールチップが「種類 / ファイル名 / 場所 / 状態」の 4 行形式で表示される（v2.6.4）
- [ ] 無題タブのツールチップに「ファイル名: 未保存（無題）」「場所: —」「状態: 未保存」が表示される（v2.6.4）
- [ ] Temp タブのツールチップに「種類: TempNest」「説明: 一時メモ」「保存: 自動保存」が表示される（v2.6.4）

---

## 6. About / バージョン表示確認

- [ ] About に「試験統合版」という古い文言が含まれていない（v1.21.0 で削除済み）
- [ ] About に「統合検証中」という古い文言が含まれていない
- [ ] バージョン番号が最新版になっている

---

## 7. ドキュメント確認

- [ ] README に NestSuite の起動方法が記載されている
- [ ] `docs/guide/nestsuite-user-guide.md` が存在する
- [ ] `docs/design/nestsuite-known-limitations.md` が存在する
- [ ] `docs/release-notes.md` に最新バージョンエントリが記載されている

---

## 8. シングルインスタンス・関連付け起動確認（v1.18.1 / v1.18.2）

- [ ] NestSuite 起動済みの状態で `.notenest` をダブルクリックすると既存ウィンドウに NoteNest タブが追加される
- [ ] NestSuite 起動済みの状態で `.chatnest` をダブルクリックすると既存ウィンドウに ChatNest タブが追加される
- [ ] NestSuite 起動済みの状態で `.ideanest` をダブルクリックすると既存ウィンドウに IdeaNest タブが追加される
- [ ] 既に開いているファイルを再度開くと既存タブがアクティブになる（重複タブなし）
- [ ] 既存ウィンドウが最小化されている場合、ファイルを受け取ると前面表示される
- [ ] ファイル指定なしで 2 プロセス目を起動した場合、既存ウィンドウに変化がない
- [ ] `--classic-notenest` を指定しても NestSuite のシングルインスタンス制御が通常どおり動作する（v1.19.3 で廃止済み・未知フラグとして無視されるため、通常の NestSuite 起動経路に入り primary 判定・pipe 転送が有効）

### 未起動時の関連付け起動＋タブ復元（v1.18.2）

- [ ] NestSuite 未起動状態で `.notenest` をダブルクリックすると、前回タブが復元されたうえで対象ファイルが追加タブとして開く
- [ ] NestSuite 未起動状態で `.chatnest` をダブルクリックすると、前回タブが復元されたうえで対象ファイルが追加タブとして開く
- [ ] NestSuite 未起動状態で `.ideanest` をダブルクリックすると、前回タブが復元されたうえで対象ファイルが追加タブとして開く
- [ ] 追加で開いたファイルのタブがアクティブになる
- [ ] 引数指定ファイルが復元済みタブと同じ場合は重複タブを作らず復元済みタブがアクティブになる

---

## 9. NoteNest Workspace エディタ確認（v2.5.7〜）

### 行番号表示（v2.5.10）

- [ ] NoteNest Workspace を開くと、本文エディタ左側に行番号が表示される
- [ ] 新規（無題）NoteNest タブを作成しても行番号が表示される
- [ ] 既存 `.notenest` ファイルを開いても行番号が表示される
- [ ] 過去に行番号非表示設定が保存されていた環境でも行番号が表示される
- [ ] 本文をスクロールしても行番号とずれない（スクロール同期が維持される）
- [ ] 行番号表示の ON/OFF UI（メニュー・チェックボックス等）が追加されていない

### 現在行強調（v2.5.8）

- [ ] 行番号ガター上でカーソルがある行が背景ハイライトされる
- [ ] ライトテーマ・ダークテーマ両方で現在行強調が確認できる

### 簡易ノートリンク補完（v2.5.9）

- [ ] `[[` を入力するとノートタイトルの候補 Popup が表示される
- [ ] ↑↓ キーで候補を移動できる
- [ ] Tab キーで `[[ノート名]]` が挿入される
- [ ] Esc キーでキャンセルできる
- [ ] タスクコメント編集中は補完が抑制される

---

## 10. TempNest 固定タブ確認（v2.6.0）

### 基本表示

- [ ] タブストリップの左端に「Temp」タブが常に表示される
- [ ] 「Temp」タブに × ボタンが表示されない
- [ ] 「Temp」タブを右クリックすると「このタブを閉じる」が無効（グレーアウト）になっている
- [ ] 「Temp」タブのカラーアクセントがグレー系（他の Workspace と異なる色）で表示される
- [ ] 「Temp」タブを中クリックしても閉じない
- [ ] 「Temp」タブをドラッグしても移動できない

### TempNest Workspace 表示

- [ ] 「Temp」タブを選択すると 2×2 の 4 スロットが表示される
- [ ] 各スロットにタイトル欄（1 行）・本文欄（複数行）・「コピー」ボタン・「クリア」ボタンがある
- [ ] ステータスバーの右端に「/ TempNest」が表示される
- [ ] TempNest 選択中はツールランチャーボタン（NoteNest/IdeaNest/ChatNest）のハイライトがなくなる

### メモ操作

- [ ] タイトル欄に入力できる
- [ ] 本文欄に入力でき、Enter で改行できる
- [ ] 「コピー」ボタンをクリックすると本文がクリップボードにコピーされる
- [ ] 本文が空の場合「コピー」ボタンが無効になる
- [ ] 「クリア」ボタンをクリックするとタイトルと本文が空になる

### 自動保存・起動時復元

- [ ] 入力後 1 秒程度待つと自動保存される（アプリを再起動すると内容が保持されている）
- [ ] アプリ終了時にも保存される（終了直前に入力した内容が再起動後に残っている）
- [ ] 保存ファイルは `%APPDATA%\NoteNest\tempnest.json` に作成される

### 通常操作との独立性

- [ ] TempNest タブ選択中に Ctrl+S を押しても保存ダイアログが開かない（無視される）
- [ ] TempNest は「最近使ったファイル」に登録されない
- [ ] TempNest はタブセッション復元の対象にならない（セッションファイルにパスが書き出されない）
- [ ] 「他のタブを閉じる」コンテキストメニューを選択しても Temp タブは閉じない（他の通常タブだけ閉じる）
- [ ] TempNest への変更があっても未保存確認ダイアログが表示されない（終了時・タブクローズ時）

---

## 11. TempNest 初期改善確認（v2.6.1）

### SH-16: 起動時ちらつき抑制

- [ ] セッション復元あり起動で、Temp タブが一瞬表示されてから前回タブに切り替わるちらつきが発生しない
- [ ] セッション復元なし起動で、Temp タブが最初からアクティブになる

### TN-1: スロットのプレースホルダーテキスト

- [ ] 各スロットのタイトル欄が空のとき「タイトル」とプレースホルダーが薄い文字で表示される
- [ ] 各スロットの本文欄が空のとき「一時メモ」とプレースホルダーが薄い文字で表示される
- [ ] タイトル欄に文字を入力するとプレースホルダーが消える
- [ ] 本文欄に文字を入力するとプレースホルダーが消える
- [ ] プレースホルダー上をクリックすると TextBox がフォーカスを受け取る（`IsHitTestVisible="False"` により透過）
- [ ] 入力をすべて削除するとプレースホルダーが再表示される
- [ ] プレースホルダーはライト・ダーク両テーマで適切な `MutedFg` 色で表示される

### TD-9: TempNestWorkspaceViewModel IDisposable

- [ ] アプリ終了時にエラーが発生しない（タイマー・イベント解除が正常に動作する）

---

## 12. 品質改善・空状態ガイド確認（v2.6.2）

### TN-5: スロットコピーボタンの完了フィードバック

- [ ] TempNest スロットの「コピー」ボタンを押すと「コピーしました」テキストがボタン行に表示される
- [ ] 「コピーしました」テキストは約 1.5 秒後に自動的に消える
- [ ] 4 つのスロット全てで同様の動作が確認できる

### TN-6: 空スロットのコピー・クリアボタン無効化

- [ ] 本文が空のスロットでは「コピー」ボタンがグレーアウト（無効）になっている
- [ ] タイトルと本文がともに空のスロットでは「クリア」ボタンがグレーアウト（無効）になっている
- [ ] タイトルのみ入力がある場合、「コピー」ボタンは無効・「クリア」ボタンは有効になっている
- [ ] 本文に入力すると「コピー」ボタンが有効になる
- [ ] クリアボタンを押してタイトル・本文が空になると、直後に「クリア」ボタンが無効になる

### L13: NoteNest ノート0件時の空状態ガイドテキスト

- [ ] 新規 NoteNest タブを作成し、ノートが0件の状態で左ペインのノートリスト欄に「＋ で最初のノートを作成」が薄い文字で中央表示される
- [ ] ノートを1件追加するとガイドテキストが消える
- [ ] ノートを全て削除するとガイドテキストが再表示される

### ID-11: IdeaNest 空状態テキスト（確認）

- [ ] IdeaNest でカードが0件のとき「まだアイデアがありません」とサブテキストが中央表示される
- [ ] 検索・フィルタ結果が0件のとき「条件に一致するカードがありません」とサブテキストが中央表示される
- [ ] カードを追加すると空状態テキストが消える

---

## 13. タブ表示整理・ツールチップ改善確認（v2.6.4）

### SH-14: タブ見出し整形

- [ ] NoteNest タブの見出しが「📝 ファイル名（拡張子なし）」形式で表示される（例：`📝 業務改善`）
- [ ] ChatNest タブの見出しが「💬 ファイル名（拡張子なし）」形式で表示される（例：`💬 開発メモ`）
- [ ] IdeaNest タブの見出しが「💡 ファイル名（拡張子なし）」形式で表示される（例：`💡 ツール改修`）
- [ ] Temp タブの見出しが「Temp」のみで表示される（絵文字プレフィックスなし）
- [ ] 無題タブの見出しが「📝 無題」のように拡張子なし・プレフィックス付きで表示される

### SH-14: タブツールチップ整形

- [ ] 保存済みタブのツールチップが「種類 / ファイル名 / 場所 / 状態: 保存済み」の 4 行形式で表示される
- [ ] 未保存変更ありタブのツールチップの状態欄が「未保存の変更あり」になっている
- [ ] 未保存（無題）タブのツールチップが「ファイル名: 未保存（無題）/ 場所: — / 状態: 未保存」形式で表示される
- [ ] Temp タブのツールチップが「種類: TempNest / 説明: 一時メモ / 保存: 自動保存」形式で表示される

---

## 14. タブドラッグ挿入位置インジケーター確認（v2.6.5）

### SH-17: インジケーター表示

- [ ] 通常タブをドラッグすると、挿入予定位置に細い縦線インジケーター（青色）が表示される
- [ ] マウス位置に応じてインジケーターがリアルタイムに移動する
- [ ] 通常タブ末尾（最後のタブの右端）への挿入位置も表示される
- [ ] ライトテーマ・ダークテーマ両方でインジケーターが視認できる

### SH-17: Drop 後の位置一致

- [ ] 通常タブを左へドラッグしてドロップすると、インジケーターが示す位置にタブが移動する
- [ ] 通常タブを右へドラッグしてドロップすると、インジケーターが示す位置にタブが移動する
- [ ] 末尾にドロップすると最後に移動する

### SH-17: Temp タブとの関係

- [ ] Temp タブ上でドラッグしても Temp タブの左側にはインジケーターが表示されない（index 1 以降のみ）
- [ ] Temp タブ自体はドラッグできない（左端固定のまま）
- [ ] 通常タブを Temp タブより左に移動できない

### SH-17: インジケーター消去

- [ ] ドロップ後にインジケーターが消える
- [ ] Esc キーでドラッグをキャンセルするとインジケーターが消える
- [ ] タブストリップ外にドロップ（またはドラッグを止める）とインジケーターが消える

---

## 15. NoteNest ノート複製確認（v2.6.6）

### L11: 複製基本動作

- [ ] ノート一覧でノートを右クリックすると「複製」メニューが表示される
- [ ] 「複製」をクリックすると同じノートブック内に新ノートが追加される
- [ ] 複製後、新ノートが選択状態になり本文エディタに内容が表示される
- [ ] 複製後のタイトルが「元タイトル のコピー」形式になっている

### L11: タイトル重複処理

- [ ] 同名の複製が既にある場合、2 回目の複製タイトルが「元タイトル のコピー 2」になる
- [ ] 3 回目は「元タイトル のコピー 3」と連番が付く

### L11: 複製内容・複製元の保全

- [ ] 本文（マーカー・タスクを含む）が複製される
- [ ] 複製後ノートのタスク・マーカーが右ペインに正しく反映される
- [ ] 複製元ノートのタイトル・本文に変化がない
- [ ] 複製後ノートを編集しても複製元ノートに影響しない
- [ ] 複製後ノートを保存すると未保存マーク（●）が消える

---

## 16. NoteNest リンク切れ手動チェック確認（v2.6.7）

### M11: 基本動作

- [ ] 左ペインの「＋」ボタンをクリックすると「リンク切れを確認...」メニュー項目が表示される
- [ ] 「リンク切れを確認...」を選択するとダイアログが開く
- [ ] リンク切れが存在する場合、ソースノート名・リンク名・行番号・行の内容が一覧表示される
- [ ] リンク切れが 0 件の場合、「リンク切れはありません ✓」メッセージが表示される

### M11: ダイアログ操作

- [ ] 「このノートへ移動」をクリックすると対象ソースノートへ移動しダイアログが閉じる
- [ ] 行をダブルクリックしても「このノートへ移動」と同じ動作になる
- [ ] リンク切れ 0 件のとき「このノートへ移動」が無効（グレー）になっている
- [ ] 「閉じる」または Esc でダイアログが閉じる（移動なし）

### M11: リンク検出ルール

- [ ] 存在するノートタイトルへの `[[リンク名]]` はリンク切れ扱いにならない
- [ ] 存在しないノートタイトルへの `[[リンク名]]` がリンク切れとして検出される
- [ ] タイトル照合が大文字小文字を無視する（`[[note a]]` と「Note A」は一致する）
- [ ] `[[ ]]`（空白のみリンク）はリンク切れ扱いにならない
- [ ] 通常の URL や Markdown リンクはリンク切れ扱いにならない

---

## 17. NoteNest ノート名変更リンク影響警告確認（v2.6.8）

### M9: 警告あり／続行

- [ ] 他ノートに `[[旧ノート名]]` が存在する状態でノートを右クリック → 名前変更を開始する
- [ ] 新しい名前を入力して確定すると「リンク影響の警告」ダイアログが表示される
- [ ] ダイアログに旧名・影響件数・参照元ノート名が表示される
- [ ] 「はい」（続行）を選択するとリネームが実行される
- [ ] リネーム後、`[[旧ノート名]]` は自動修復されず文字列のまま残る

### M9: 警告あり／キャンセル

- [ ] 「いいえ」（キャンセル）を選択するとリネームされない
- [ ] 元のノート名がそのまま維持される

### M9: 警告なし（バックリンクなし）

- [ ] 他ノートに参照が存在しない場合は警告なしで即時リネームされる

### M9: 大文字小文字のみの変更

- [ ] `Meeting` → `meeting` のような大文字小文字のみの変更では警告が出ない
- [ ] リネーム自体は通常通り実行される

### M9: 重複名チェックとの共存

- [ ] 既に存在するノート名への変更は「既に使用されています」エラーになる（警告より優先）
- [ ] M11「リンク切れを確認...」の動作に影響しない
