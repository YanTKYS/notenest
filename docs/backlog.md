# Backlog — NoteNest

v0.2.0 以降の検討候補を整理します。
設計判断の背景は [docs/design-decisions.md](design-decisions.md) を参照してください。

---

## v0.5.0 以降の候補（優先度 高）

| 項目 | 概要 |
|------|------|
| サンプル導線の改善 | 初回起動時のバナー文言や案内フローを見直す |

---

## v0.5.0 以降の候補（優先度 中）

実装コストと利便性を比較しながら判断します。

| 項目 | 概要 |
|------|------|
| タスクのドラッグ並べ替え | ドラッグ＆ドロップでタスクを並べ替える |
| 行番号表示 | エディタ左に行番号を表示する（TextBox では難しいため部品変更が必要な可能性あり） |

---

## 将来検討

実装に相応の設計変更が必要なもの。必要性が明確になった段階で判断します。

| 項目 | 概要・前提 |
|------|-----------|
| マーカー行の表示／非表示 | WPF TextBox では安全な実装が難しい。エディタ部品の変更が前提になる。詳細は本ドキュメント下部参照 |
| Markdown プレビュー | WebView2 または AvalonEdit の採用が必要。依存関係が増える |
| シンタックスハイライト | 高機能エディタ部品が必要 |
| ノート間リンク | `[[ノート名]]` 記法などを定義してノート間を参照できるようにする |
| タスクとノートの関連付け | タスクから特定ノートへのリンクを設定できるようにする |
| エクスポート機能 | Markdown ファイルや PDF へのエクスポート |
| テーマ切り替え | ダーク・ライトテーマの選択 |
| 自動保存 | 一定間隔での自動保存（ただし保存ダイアログの競合に注意が必要） |
| タスクへの期限・優先度設定 | タスクモデルの拡張が必要 |
| 通知機能 | タスク期限などの通知（タスク期限設定が前提） |
| Git 連携 | `.notenest` ファイルの変更履歴管理 |
| ノートブック間のノート移動 | ドラッグ＆ドロップでノートブックをまたいで移動 |

---

## 対象外または当面見送り

NoteNest の設計方針から意図的に除外しているもの。

| 機能 | 理由 |
|------|------|
| 画像貼り付け | 軽量テキスト管理ツールの軸がぶれる。単一 JSON ファイルへの画像埋め込みはファイルサイズ増大を招く |
| 共同編集 | ローカル単一ファイル管理の方針と根本的に合わない。排他制御・マージ処理が複雑 |
| クラウド同期 | ローカル利用を前提としている。OneDrive 等のフォルダへ手動配置することで代替可能 |
| 文字数表示 | 現時点の主要価値（プロジェクト管理）に対して優先度が低い |
| 高機能 Markdown エディタ化 | エディタ部品の差し替えは影響範囲が大きい。安定性優先のため慎重に判断 |
| 添付ファイル管理 | 単一 JSON ファイルとの相性が悪い |
| バックアップ自動化 | `.notenest` ファイルのコピーで代替可能。アプリ内実装は過剰 |
| 複数プロジェクトのタブ管理 | 現在の多重起動で代替可能 |

---

## 将来検討：マーカー行の表示／非表示

本文中の `[TODO]` `[FIXME]` `[NOTE]` を含む行を一時的に非表示にする要望がある。

**v0.1.x では実装しない。** 理由は以下のとおり。

- WPF 標準 TextBox では、特定行だけを安全に非表示にする実装が難しい
- 表示用テキストと保存用本文が乖離し、本文消失リスクがある
- カーソル位置・検索・マーカークリック移動との整合が複雑になる

**将来的に検討する場合の前提：**

- 非表示中は編集不可にする
- 表示用ビューと保存用本文を明確に分離する
- TextBox ではなく、行単位表示に適したエディタ部品を採用する
- マーカー行を非表示にするのではなく、マーカー一覧側のフィルタ機能で代替することを優先する
