<Window x:Class="IdeaNest.Views.WorkspaceHostPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:IdeaNest.Views"
        Title="WorkspaceView ホスト表示テスト"
        Width="980" Height="640" MinWidth="780" MinHeight="480"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource AppBackgroundBrush}">
    <DockPanel>
        <Border DockPanel.Dock="Top" Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1"
                Padding="12,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                           Text="Hosted Preview — ファイル保存・自動保存なし"
                           Foreground="{StaticResource TextSecondaryBrush}" />
                <TextBlock Grid.Column="1" Text="Menu: Hidden" Margin="16,0,0,0"
                           Foreground="{StaticResource TextSecondaryBrush}" />
                <TextBlock Grid.Column="2" Text="{Binding CardStatus}" Margin="16,0,0,0"
                           Foreground="{StaticResource TextSecondaryBrush}" />
                <TextBlock Grid.Column="3" Text="{Binding FilterStatus}" Margin="16,0,0,0"
                           Foreground="{StaticResource TextSecondaryBrush}" />
                <TextBlock Grid.Column="4" Text="{Binding DirtyStatus}" Margin="16,0,0,0"
                           Foreground="{StaticResource TextSecondaryBrush}" />
            </Grid>
        </Border>
        <views:IdeaNestWorkspaceView x:Name="WorkspaceView"
                                     DataContext="{Binding Workspace}"
                                     ShowMenu="False" />
    </DockPanel>
</Window>
